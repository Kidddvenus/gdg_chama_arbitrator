# Testing Rules

- Use `pytest` for all tests.
- Mock all external API calls (Vertex AI, OpenAI, etc.).
- Coverage: Maintain at least 80% coverage on core services.
- Integration tests must run in isolated Docker environments.
